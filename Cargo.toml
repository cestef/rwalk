[workspace]
members = ["crates/rwalk", "crates/set-field"]
resolver = "3"

[profile.dev]
debug = false

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
# strip = true
# lto = true
# codegen-units = 1
